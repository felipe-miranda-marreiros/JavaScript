//podemos usar arrays com for loop

const felipeArray = [
    'Felipe',
    'Miranda',
    2021 - 1997,
    'Front-End',
    ['Amanda', 'Jéssica','Liana']
];

for(let i = 0; i < 5; i++){ //começamos com 0 porque arrays são zero-based
    console.log(felipeArray[i]); //em vez de escrevermos a posição de cada elemento da array, usamos a variável.
};